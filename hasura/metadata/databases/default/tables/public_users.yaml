table:
  name: users
  schema: public
array_relationships:
  - name: albums
    using:
      foreign_key_constraint_on:
        column: created_by
        table:
          name: album
          schema: public
select_permissions:
  - role: user
    permission:
      columns: []
      filter:
        id:
          _eq: X-Hasura-User-Id
